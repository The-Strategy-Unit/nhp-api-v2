name: Deploy Azure Functions to Development

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yaml
    with:
      environment: development
    secrets: inherit
